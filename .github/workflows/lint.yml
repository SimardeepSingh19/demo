name: lint
on: pull_request
  # - branches: main

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    # - run: chmod +x script/lint
    - run: chmod +x script/python.py
    - run: brew install yq
    # - run: script/lint 
    - run: script/python.py