name: lint
on: pull_request
  # - branches: main

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - run: chmod +x script/lint
    - run: sudo apt-get update
    - run: brew install yq
    - run: script/lint 
