name: lint
on: pull_request
  # - branches: main

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - run: chmod +x script/lint
    - run: brew install yq
    - run: script/lint 